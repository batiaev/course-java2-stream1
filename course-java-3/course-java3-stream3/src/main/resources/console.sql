select * from Students

-- CREATE TABLE IF NOT EXISTS Students
-- (
--   StudID INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
--   Name TEXT NOT NULL,
--   GroupName TEXT NOT NULL,
--   Score INTEGER NOT NULL
-- );

-- INSERT INTO Students (Name, GroupName, Score) VALUES ("Bob", "Tbz11", 80);

-- UPDATE Students SET Score = 90, GroupName = 'test' WHERE Name = "Bob";

-- DELETE FROM Students WHERE StudID = 1;